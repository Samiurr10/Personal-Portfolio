# Frontend (set in Vercel → Project → Settings → Environment Variables)
REACT_APP_SITE_URL=https://samiur.dev
# Optional override (default /api/search on same domain)
REACT_APP_API_SEARCH_URL=

# Vercel → Project → Environment Variables (server-side, not REACT_APP_)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
REACT_APP_CONTACT_EMAIL=srahman96@gatech.edu

# Backend (separate deploy or local only)
OPENAI_API_KEY=
PINECONE_API_KEY=
